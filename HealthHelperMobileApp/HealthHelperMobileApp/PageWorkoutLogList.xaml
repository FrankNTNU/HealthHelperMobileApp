<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HealthHelperMobileApp.PageWorkoutLogList">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="瀏覽運動紀錄" FontSize="30"
                VerticalOptions="Start" 
                HorizontalOptions="FillAndExpand" />
            <CollectionView x:Name="cView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Label Text="{Binding name}" FontSize="20"></Label>
                            <Label Text="{Binding name}" FontSize="20"></Label>
                            <Label Text="{Binding name}" FontSize="20"></Label>
                            <Label Text="{Binding EditDate}" FontSize="15"></Label>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>